import React from 'react'
import TransferToken from '../../../domains/transaction/components/TransferToken/TransferToken.impl'

const Transfer = () => {
    return (
        <TransferToken />
    );
}

export default Transfer;